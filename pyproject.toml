[project]
name = "flask-mongodb-prac"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "SaquibAnjum", email = "saquib1312@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "pymongo>=4.18.1",
]

[project.scripts]
flask-mongodb-prac = "flask_mongodb_prac:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
